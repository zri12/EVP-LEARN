# DESIGN.md — Entry Point

Canonical APK design specification:

```text
docs/DESIGN.md
```

The actual approved visual reference is:

```text
UI-APK PEMB B.INGGRIS/
```

Do not maintain a second independent design specification in this root file. Update `docs/DESIGN.md` so Codex has one source of truth.
