# CLAUDE.md

Codex in VS Code is the primary agent for this project. This file is retained only as a compatibility entry point for other coding agents.

Before doing any work, follow:

1. `./AGENTS.md`
2. `./docs/INDEX.md`
3. `./docs/DECISIONS.md`
4. task-specific documents under `./docs/`

Important:
- `./UI-APK PEMB B.INGGRIS/` is a read-only approved prototype reference.
- The final application is Flutter/Dart Android, not React/Vite.
- Do not package the prototype as a WebView final APK.
- Do not change academic content or scoring from assumptions.
- When documents conflict, use the precedence defined in `AGENTS.md`.
